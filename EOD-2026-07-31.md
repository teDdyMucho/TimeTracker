# End of Day — 31 July 2026
**Project:** Timevera (BuildOne) Time Tracker — Admin Portal + Mobile App

---

## Summary
Reviewed the client's meeting-agenda feedback and knocked out the admin enhancements (Messages unread badge, individual employee timesheet PDF, dashboard speed-up), fixed a tsconfig deprecation, and — the big one — **set up Firebase FCM so Android push notifications will finally work**. Also submitted **iOS v1.0.1 with messaging** for App Store review earlier in the day.

---

## 1. iOS — v1.0.1 with messaging submitted for review ✅
- The live App Store build was still **Build 1 (no messaging)** — that's why the "iOS messaging icon is invisible" feedback: the icon was added after Build 1.
- Bumped to **v1.0.0 / build 3**, built via EAS, submitted to App Store Connect, created **version 1.0.1**, attached build 3, and clicked **Add for Review**.
- Status: **Waiting for Review** (~1–2 days). Once approved, the iOS messaging icon "bug" resolves automatically — it's just an old build on the store.

## 2. Client meeting-agenda review — triaged
Confirmed working (no action): clock in/out real-time sync, reset password, approve/reject/add attendance, approve/reject overtime.

Bugs identified:
- **iOS messaging icon invisible** → NOT a bug; old build. Fixed by the v1.0.1 submission above.
- **Android no push notifications** → real bug; root cause = no Firebase/FCM (see #6).

## 3. Admin — Messages unread badge ✅
Red badge on the **Messages** nav item showing the count of unread employee messages (mirrors the Overtime badge). Files: `layout.tsx`, `app-shell.tsx`, `sidebar.tsx`.

## 4. Admin — Individual employee fortnightly timesheet PDF ✅
On **Reports**, pick an employee + date range → **"Timesheet PDF"** button → branded PDF matching the ARKO sample: Date, Start (clock in), Finish (clock out), Location (Onsite/Offsite), Project, Regular/Overtime/Total hours (OT = beyond 8h/day), totals, and a certification + signature block. Enlarged the Timevera logo per feedback. Files: `lib/timesheet-pdf.tsx`, `api/reports/timesheet/route.ts`, `reports-filter.tsx`.

## 5. Admin — Dashboard performance ✅
The dashboard ran `supabase.auth.getUser()` sequentially before all other queries (blocking them), plus a separate profile-name query. Moved `getUser()` into the parallel `Promise.all` and took the greeting name from the auth user's metadata — removed 2 round-trips. Real-time data kept. File: `(dashboard)/page.tsx`.

## 6. Mobile — Android push (FCM) set up ✅ (build pending)
The real Android push bug: **no Firebase Cloud Messaging** was configured (iOS push worked via APNs; Android had no delivery channel).
- Created Firebase project **`timevera-baf99`**, registered the Android app (`com.timevera.timesheets`), downloaded `google-services.json` into `mobile/` (gitignored — contains keys).
- Generated a Firebase **Service Account key** and uploaded it to EAS via `eas credentials` → **FCM V1 configured** (confirmed on EAS).
- Updated `app.json`: added `android.googleServicesFile` + `POST_NOTIFICATIONS` permission.
- **Next: rebuild the Android APK** — the new build will have working push.

## 7. tsconfig fix ✅
Removed the deprecated `baseUrl` from `admin/tsconfig.json` (deprecated in TS 7.0). Path aliases still resolve via `moduleResolution: bundler`. Verified no type errors.

---

## Outstanding / Next steps
1. **Rebuild the Android APK** (`eas build --platform android --profile preview`) → push notifications will work. Then upload the new APK to Google Drive, get the new file ID, and update `ANDROID_APK_FILE_ID` in `downloads/install-links.tsx`.
2. **Test Android push:** install the new APK → send a message from admin → confirm the notification arrives.
3. **Wait for Apple** to approve iOS v1.0.1 → the iOS messaging icon appears once users update.
4. **Labour Hire entity** — ON HOLD pending Robbie's confirmation (is it a new company entity, or an employee tag?). Don't build until clarified.
5. **CRM lead form** — separate review with Robbie; not a dev task yet.

---

## Notes
- Never commit `google-services.json` or the Service Account key — both are gitignored / stored in EAS, not git.
- Admin changes (badge, timesheet PDF, dashboard perf, tsconfig) deploy on push (Netlify) — no rebuild.
- The Android push fix needs the rebuild to take effect; it's config + credentials only right now.
