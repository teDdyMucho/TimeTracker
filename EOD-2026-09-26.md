# End of Day — 26 September 2026
**Project:** Timevera (BuildOne) Time Tracker — Admin Portal + Mobile App

---

## Summary
**ARKO Joinery is now connected to Xero** and linked to its own organisation. Added support for one Xero app per company. Build One is still not connected; the credentials supplied today belong to a Web app, not a Custom Connection. Fixed a duplicate-key error in Reports and the dashboard.

---

## 1. Xero: one app per company ✅ (`b1c800d`)
- A Xero Custom Connection is tied to one organisation, so each company needs its own app. The admin now reads multiple apps from env: `XERO_CLIENT_ID`/`XERO_CLIENT_SECRET` for Build One (default), plus `XERO_CLIENT_ID_<NAME>`/`XERO_CLIENT_SECRET_<NAME>` (e.g. `_ARKO`). Each push uses the app authorised on that company's org.
- **Reconnect** now links a company to the authorised org whose name matches. It refuses to guess when no org matches or several do. Before this, both companies had been linked to Build One's tenant.
- When an org isn't authorised, the error now names the orgs that are, instead of a bare 403.
- Tested 12/12 against a fake two-app Xero (routing, name matching, ambiguity, one broken app not affecting the other).

## 2. ARKO Joinery ✅ connected
- New ARKO Custom Connection credentials tested: all four scopes granted, org **ARKO Joinery Pty Ltd**, **9 payroll employees**.
- Saved to `.env.local` (gitignored) and Netlify as `XERO_CLIENT_ID_ARKO` / `XERO_CLIENT_SECRET_ARKO`; redeployed.
- ARKO entity re-linked from Build One's tenant to its own (`9c52a1d5-…`). Verified end-to-end through the real admin code.

## 3. Build One ❌ still not connected
- The Build One Custom Connection (`D894AF…`) still has no authorised organisation.
- The credentials supplied (`171DA…`) are the original **Web app** (secret created 24 June; has redirect URIs, "0 of 5 connections"). Its token isn't tied to any org, so it can't read or push payroll. **Not used or stored.**
- Fix: Chris re-authorises the `D894AF…` Custom Connection, or a new Custom Connection is created like ARKO's. No code changes needed after that. (Alternative: build an OAuth login flow for the Web app. Refresh tokens lapse after 60 days unused. Not recommended.)

## 4. Reports / dashboard duplicate key ✅ (`d9c5bae`)
- ARKO and Build One each have a project called "Project Management/ Admin". The Reports table and dashboard "Top projects" keyed rows by name, which caused React's duplicate-key error and two identical rows. Rows are now keyed by project id, and a name shared by several companies shows the company. Hours were never mixed; grouping was already by id.

---

## Outstanding / Next steps
1. **Build One Xero:** re-authorise the Custom Connection (`D894AF…`) or create a new one; send Client ID + secret.
2. **Rotate secrets:** the ARKO and Build One secrets were pasted in chat. Generate new ones once everything works.
3. **Duplicate timesheets** (Ronny 12.00h, Robbie 16.00h, 24 Sep): still waiting for your go-ahead to delete before the pay run.
4. **Android 7.3 APK** not built yet; Android still has the duplicate-timesheet race.
5. **App Store Connect:** attach build 11 to 7.3 → Submit for Review.
6. **Flat rate:** tick the correct Robbie and Chris profiles.
7. **Netlify auto-deploy** still broken; deploys are triggered via API.
8. **APK link expires 6 Oct.** Move it to Google Drive.
