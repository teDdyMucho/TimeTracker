# End of Day — 5 September 2026
**Project:** Timevera (BuildOne) Time Tracker — Admin Portal + Mobile App

---

## Summary
Closed out the Build One Xero rollout (all review concerns done + a client status report), started the ARKO Joinery Xero connection, shipped a mobile enhancement (project name on time logs) requested by Chanelle, and cut fresh **v1.0.4** builds for both iOS and Android.

---

## 1. Build One Xero — signed off ✅
- Verified all five review concerns are complete: PDF logos, employee invite email (webhook), custom pay-run dates, Xero push JSON error fix, and projects tab (active first + active-only count — the tab count fix landed today).
- Produced a client-facing **status report** (Build One connected & working; ARKO still needs its own invite). Delivered both as an on-screen report and a formatted summary.

## 2. ARKO Joinery Xero — connection started ⏳
- Created a second Xero **Custom Connection** app for ARKO ("Custom connection" integration type, same as Build One).
- Set the four scopes (identical to Build One): `payroll.employees`, `payroll.timesheets`, `payroll.settings.read`, `accounting.settings.read`.
- Authorised email set to **chris@arkojoinery.com.au**. Status: **waiting for authorisation** — Chris still needs to accept and approve on ARKO's Xero org.
- **Note for later:** the app code currently supports one Xero connection (Build One). Once ARKO's Client ID + Secret are in hand, the code needs multi-org support (pick the right credentials per business entity).

## 3. Mobile — project name on time logs ✅ (Chanelle's request)
- Requirement: employees' time logs should show the **specific project name**, not just Onsite/Factory.
- `fetchRecentTimesheets` now joins `projects(name)`; the History screen and the home "Recent" list lead with the project name, with work location + status beneath.
- Verified end-to-end against the live DB (join returns the project) and confirmed the `projects` RLS policy lets any signed-in employee read projects, so it works in the app.
- Committed + pushed.

## 4. New builds — iOS + Android v1.0.4 ✅
- Bumped app version to **1.0.4**.
- **iOS:** built (Build 6) and submitted to App Store Connect → build attached to the 1.0.4 version, ready to Add for Review.
- **Android:** built a new **APK** (v1.0.4) via the preview profile. APK artifact ready to download.

---

## Outstanding / Next steps
1. **iOS:** finish the App Store submission — Save → Add for Review → answer export-compliance (No) & IDFA (No) → Submit. Wait for Apple review (~1–3 days).
2. **Android:** download the v1.0.4 APK, upload to Google Drive (Anyone with the link), grab the file ID, and update the install page's `ANDROID_APK_FILE_ID`.
3. **ARKO Xero:** Chris authorises ARKO's connection → get ARKO's Client ID + Secret → add multi-org support to the app so ARKO can push too.
4. **Netlify redeploy** (+ Xero env vars) so all recent admin changes go live.
5. **GoHighLevel automation** on the invite webhook so new-employee emails actually send.

---

## Notes
- iOS build 6 / Android APK both carry v1.0.4 with the project-name feature.
- Android v1.0.4 APK URL (this build): expo.dev artifact `MBvNWACuWL6YgkXdeDB4m3NtRjrqmZonY4gq07t2OTU.apk`.
- Custom Connection = one Xero org per app, so Build One and ARKO each need their own connection + credentials.
- `.env.local` (Xero secret) stays gitignored — live secrets set separately in Netlify.
