# End of Day — 7 August 2026
**Project:** Timevera (BuildOne) Time Tracker — Admin Portal + Mobile App

---

## Summary
Cleared the entire client meeting-agenda task list — new **Leave** feature (mobile submit + admin approve), **switch projects mid-workday**, fixed the **nightly session reset**, extended auto clock-out to **12h**, plus a batch of admin fixes (deactivate/archive buttons, AEST timezone, Labour Hire type, attendance date-range + timesheet report, Messages badge). Cleaned a Firebase secret out of git history, then **built & submitted iOS v1.0.2** to the App Store and **rebuilt the Android APK** with working push.

---

## 1. Mobile — new features & fixes ✅
- **Leave requests (end-to-end):** mobile "Request Leave" screen (Annual/Sick/Personal/Unpaid + from/to dates + reason) with history; admin **Leave** tab to approve/reject; employee gets in-app + push notification on the decision. New DB table `leave_requests` (migration 0016).
- **Switch Project mid-workday:** "Switch Project" button on the active-session card — closes the current project (logs its hours) and clocks into a new project without a new selfie, so each project gets its own timesheet segment.
- **Session "resets to T" nightly — FIXED:** React Native stalls Supabase's token auto-refresh in the background; added an `AppState` listener that refreshes on foreground. No more overnight logout.
- **Auto clock-out 8h → 12h:** longer construction shifts no longer auto-close early.

## 2. Admin — fixes & features ✅
- **Deactivate (employees) / Archive (projects) — FIXED:** the code wrote status `'inactive'`, but the enum is `('active','archived')`, so it silently failed. Now `'archived'`.
- **Timezone → AEST:** attendance/overtime "today" + clock-in/out times + message timestamps now render in Australia/Brisbane (were UTC on Netlify).
- **Attendance date-range filter + per-employee timesheet PDF** (matches the ARKO sample layout).
- **Labour Hire employment type** added (migration 0015 + dropdowns + badge).
- **Messages unread badge** + **Leave pending badge** in the sidebar.
- Removed deprecated `baseUrl` from both admin & mobile `tsconfig.json`.

## 3. Security — Firebase secret removed from git ✅
- The **Firebase service account key** had been committed earlier; GitHub push protection blocked the push. Removed it from the **entire git history** (filter-branch), gitignored `*firebase-adminsdk*.json`, force-pushed clean history.
- **`google-services.json`** is public FCM config (not a secret) and MUST be git-tracked for EAS to build Android push — so it's tracked; only the service-account key stays ignored.
- Recommended: rotate the exposed service-account key in Firebase as a precaution.

## 4. iOS — v1.0.2 built & submitted ✅
- Bumped to **v1.0.2 (build 4)**, built via EAS, and **uploaded to App Store Connect** (`eas submit`).
- Creating App Store **version 1.0.2**, attaching build 4, adding "What's New", then **Add for Review** (in progress).
- (v1.0.1 with messaging was already **approved / Ready for Distribution**.)

## 5. Android — APK rebuild ✅ (with push)
- First attempt failed: `google-services.json` missing (it was gitignored, EAS only uploads git-tracked files). Fixed by tracking it (see #3).
- Rebuilding the Android APK via `eas build --preview` — this build finally has **working FCM push** + all the new features.

---

## Outstanding / Next steps
1. **Finish the iOS 1.0.2 submission** in App Store Connect (attach build 4 + Add for Review) → wait ~1–2 days for approval.
2. **Android APK:** once the build finishes, download the `.apk`, upload to Google Drive, and update `ANDROID_APK_FILE_ID` in `admin/.../downloads/install-links.tsx`.
3. **Test on real builds:** Android push, auto clock-out 12h, and the overnight session fix (can't be tested in Expo Go).
4. **Rotate** the exposed Firebase service-account key (precaution).
5. Still on hold: **Xero push** (wait for client to grant the connection), **Labour Hire as entity** vs employment-type (Robbie to confirm), **CRM lead form** (separate review).

---

## Notes
- Migrations `0015_labour_hire` + `0016_leave_requests` were run in Supabase.
- Push notifications only work on real builds (iOS via APNs, Android via the new FCM).
- Admin changes deploy on push (Netlify); mobile changes need the new builds.
