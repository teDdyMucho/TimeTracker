# End of Day — 19 September 2026
**Project:** Timevera (BuildOne) Time Tracker — Admin Portal + Mobile App

---

## Summary
Client email with three requests: extend the auto clock-out from 12h to 16h, pay Robbie and Chris a flat rate with no overtime, and keep employees signed in (the "T" profile). All three are now built. The admin site was finally redeployed, so weeks of admin work is live. One new problem surfaced: **Build One's Xero connection has been disconnected on the Xero side.**

---

## 1. 16-hour auto clock-out ✅
- App constant and the nightly server job both raised 12h → 16h (`0018`). Behaviour otherwise unchanged: a forgotten clock-out logs exactly one 16-hour shift (client's choice — not real elapsed time).
- Server side is **live**; verified on the production DB with a rolled-back test (13h session left open, 17h session closed at exactly 16h, 16h timesheet, correct notice text).
- App side tested with a new harness (10/10) and is in the new builds.

## 2. Flat rate — no overtime ✅ built, not yet applied to anyone
- New `profiles.flat_rate` (`0019`, default false). A flat-rate worker's hours all land on the ordinary band — no overtime, weekend or public-holiday loading — in the pay run, project costs, Xero preview and Xero push.
- Checkbox under the hourly rate on the employee form; "Flat" badge in the employee list.
- Logic tested (7/7), including: with nobody ticked, nothing changes for anyone.
- **Still to do:** tick it on the right profiles. There are three "Robbie Wynne" and two "Chris Wynne" profiles — the admin must pick the correct ones.

## 3. Stay signed in / "T" profile ✅ in v1.0.5
- The "T" is the first letter of the fallback word "there" shown when no profile loaded — not another profile. Fixed in v1.0.5 (done 18 Sep). Confirmed there is **no auto log-out** anywhere: the only sign-out is the button in Settings.

## 4. Builds
- **Android:** v1.0.5 APK with the 16h code (build `5462d1ae`, commit `3f59f7c`). Live on the install page.
- **iOS:** v1.0.5 **build 8** finished. Submission / App Store Connect status not confirmed today.
- Pre-build testing: typecheck clean in changed files, auth regression 18/18, 16h harness 10/10, Metro bundle OK. Not tested on a physical device by me.

## 5. Netlify ✅ live, auto-deploy broken
- Manual deploy published `3f59f7c`; later `dbff1b5` was deployed via the Netlify API. Live `/install` confirmed serving the new APK.
- All env vars present on Netlify; Xero secret there matches local.
- **Pushes do not trigger builds** (none since 31 Aug). Netlify settings look normal (`stop_builds: false`, GitHub App linked), so the GitHub webhook / app access is the likely cause. Until fixed, every push needs a manual deploy.
- Correction to yesterday's note: the site was not failing to deploy — no deploy had been *triggered* since 31 Aug.

## 6. ⚠️ Build One Xero connection disconnected
- Using the production credentials: token is granted with all four scopes, but `/connections` returns `[]` and the payroll API returns **403 AuthenticationUnsuccessful** with the stored tenant id. It returned Build One + 6 employees on the morning of 18 Sep.
- Cause is on the Xero side — likely Chris's changes to Custom Connection subscriptions while adding a connection for ARKO. **"Push to Xero" will fail until re-authorised.**

---

## Outstanding / Next steps
1. **Xero:** in the Xero developer portal, "Resend authorisation email" for the **Build One** app → Chris allows on the Build One org. Same for the **ARKO** app on the ARKO org. If a new secret is generated, update `.env.local` and Netlify. Then re-run the connection test before using Push to Xero.
2. **iOS:** confirm build 8 is submitted; in App Store Connect attach build 8 to the 1.0.5 record (replacing build 7) and submit for review.
3. **Flat rate:** tick the correct Robbie and Chris profiles in the admin.
4. **Get workers onto v1.0.5** — requests #1 (app side) and #3 reach nobody until installed.
5. **Fix Netlify auto-deploy** — check the repo's GitHub webhooks / re-link the repository in Netlify.
6. **APK link expires 2 Oct 2026** — move the APK to Google Drive before then.
7. **Review 16h/12h-cap timesheets before each pay run** — a forgotten clock-out is paid as a full capped shift (Robbie Wynne 16 & 17 Sep sit at 12h).
8. Still open: auto clock-out notifications from the *app* are blocked by RLS (server job notifies fine); multi-org Xero support for ARKO; GoHighLevel invite-email automation; Google Play developer account.

---

## Notes
- Do not use `supabase db push` on this project — the remote has no migration history. 0018 and 0019 were applied individually with `supabase db query --linked -f`.
- iOS build 8 is from commit `dbff1b5`; its mobile code is identical to the Android APK's `3f59f7c` (the later commit only changed an admin file).
