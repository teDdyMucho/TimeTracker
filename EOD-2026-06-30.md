# End of Day — 30 June 2026
**Project:** BuildOne Time Tracker — Admin Portal + Mobile App

---

## Summary
Big rebrand + polish day: rolled the whole product over to the **black / white / grey** identity (from the logo), made push notifications + payroll PDF export functional, fixed the overtime filter, seeded a full demo week of data, and added branded animations (login success splash, splash haptics, running-glow login card). Login was also reworked.

---

## 1. Monochrome rebrand — black / white / grey ✅
- Replaced the bronze palette with a **monochrome identity** across the **admin** (tailwind tokens + every hardcoded hex swept): sidebar/login now **pure black**, content light with **black-bordered cards**, charts use a **greyscale ramp**.
- Same rebrand applied to the **mobile app** (tailwind tokens + all screens + splash, lock screen, app icon background, notification accent).
- **Modals & dropdowns** unified as **frosted glass** over a blurred backdrop; **filter bars** clean white; **active states** dark, **destructive** red.
- Custom **grey-highlight dropdown** replaces native blue `<select>` menus everywhere (Employees, Entities, Attendance, Overtime, Reports, Projects, Payroll), with open/close animation + dark-grey hover.

## 2. Login page ✅
- Set **`login_bg.png`** as the login background (cover) with a dark overlay + vignette so content stays readable. *(Fixed middleware that was redirecting the image — now excludes static assets.)*
- Added a **running-glow border** that sweeps the login card's corners continuously.
- Added a **post-login success splash** (full-screen BuildOne logo animation → dashboard), portaled to body.
- Enhanced the dark background depth (gradient, dot grid, halos).

## 3. Attendance / Overtime ✅
- **Fixed the Overtime filter** (employee/company/date) — it was showing stale results because the client cached the list in state; now syncs on prop change.
- Fixed a real data bug: overtime decisions used a non-existent **`reviewed_at`** column → corrected to **`decided_at`** (approve/reject now persists + shows the date).
- Approve/Reject buttons given **white text** (were invisible on dark/red).

## 4. Payroll ✅
- **PDF export** (branded, server-generated via `@react-pdf/renderer`) with the BuildOne logo. Scope follows filters: **1 entity / all users**, **1 user / all entities**, **all users / 1 entity**, **all users / all entities**. Buttons on the Payroll list (per run + filtered bar) and the run detail page.
- Documented how payroll works (timesheets → day classification → OT/weekend/PH bands → gross = hours × rate × multiplier; rate snapshotted at run creation).

## 5. Mobile — push + haptics ✅
- Built the **server-side push sender** (`sendPushToProfile`) — admin approve/reject of attendance & overtime now sends a **phone push** (Android channel + sound). *(Works on an EAS build; Expo Go can't receive remote push since SDK 53 — that's the red banner, not a bug.)*
- Added **haptic feedback** to the open-splash animation (`expo-haptics`).

## 6. Demo data ✅
- SQL seed (`seed_demo_week.sql`) — a full **Mon–Sun week** of clock-ins/outs, timesheets, and overtime for **james, eunice, Joshua** (all under one entity, with pay rates), for a video walkthrough. Fixed enum casts + single-entity grouping so all 3 appear in one pay run.

## 7. Admin account + access fixes ✅
- Created/seeded the **admin@buildone.au** account (Dashboard "Add user" + `make_admin_profile.sql`); the raw-SQL `auth.users` approach 500'd, so the supported path is documented.
- Attendance "Today" now uses the server's local date (matched the seeded data's timezone).

---

## ⚠️ Open issue — LOGIN SUCCESS SPLASH GETS STUCK
- After a successful login the splash plays but can **hang** instead of going to the dashboard.
- **Fix:** `app/(auth)/login/login-form.tsx` must use a **hard navigation** on success — `window.location.assign('/')` — **not** `router.replace('/')` (the soft push races the just-set session cookie).
- This edit **keeps reverting** (an editor buffer or auto-save is overwriting the file). **Action:** open `login-form.tsx`, change `onDone={() => router.replace('/')}` to `onDone={() => { window.location.assign('/') }}`, remove the unused `useRouter`, and **save** — confirm the editor isn't holding a stale copy.

---

## Outstanding / Next steps
1. **Re-apply + save the login hard-navigation fix** (above) and verify sign-in lands on the dashboard.
2. **Run the SQL** in Supabase if not yet: `seed_demo_week.sql` (demo data), and confirm migrations `0010`–`0013` are applied.
3. **Rebuild the APK** (`eas build -p android --profile preview`) to ship today's mobile changes (monochrome theme, haptics, Android push channel). Remote push only works on the build, not Expo Go.
4. **Log in with `admin@buildone.au`** (admin) — `buildone@gmail.com` is an employee and is correctly rejected from the admin portal.
5. **Set employee pay rates** before creating pay runs (gross is $0 without a rate); recreate a run after any rate/approval change (snapshot behavior).
6. **Xero scopes** confirmed sufficient: `openid profile email offline_access payroll.employees payroll.timesheets payroll.settings` (current env also has extra payroll scopes — harmless).
