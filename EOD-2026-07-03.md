# End of Day — 3 July 2026
**Project:** Timevera (BuildOne) Time Tracker — Admin Portal + Mobile App

---

## Summary
Rebrand + polish day. Introduced the **Timevera** brand across the mobile app (name, bundle, icon) and the admin web (login, sidebar, avatars), added the **ARKO client logo** in key places, made the **push notifications + payroll PDF export functional**, fixed the **overtime filter bug**, seeded a **full demo week** of data, and did a round of UI fixes (login success splash, thinner chart line, show-password on mobile).

---

## 1. Timevera rebrand ✅
**Mobile app (full identity change):**
- App **name → Timevera**, `slug` → `timevera-timesheets`, `scheme` → `timevera`, bundle/package → `com.timevera.timesheets`.
- **App icon + native splash** → Timevera icon (`timevera-white.png`). Ran a fresh `eas init` (new projectId, since the bundle changed).
- Text updates: biometric prompt "Unlock Timevera", login placeholder `you@timevera.com`.

**Admin web:**
- **Login page:** big **Timevera** logo on the left; **BuildOne + ARKO** logos placed at the **top of the sign-in card**; "Sign in to continue to Timevera".
- **Sidebar + mobile header bar** logo → Timevera web logo.
- **Admin profile avatars** (sidebar + header dropdown) → Timevera icon in a white circle.
- **Login success splash** → Timevera logo.

## 2. Logo asset handling ✅
- Copied client logos into the apps (`admin/public` → `lib/` for bundler import; `admin/public` → `mobile/assets`).
- **Cropped** the Timevera web logo (it had huge empty transparent margins that made it render tiny) so it displays full-size — kept its **original metallic colours** (an earlier white-recolor was reverted).
- Made the ARKO login logo's baked background transparent so it sits cleanly.
- Verified the app-icon "checkerboard" the client saw is **Expo Go's placeholder** (icons only render on a real EAS build, never in Expo Go) — the icon files themselves are correct.

## 3. Mobile app polish ✅
- **Open splash** now shows **both BuildOne + ARKO** logos, appearing together, each with its own underline sweep (removed the "FOR" label).
- **Login page:** BuildOne + ARKO logos, light-grey background, **show-password eye toggle** on the password field.
- Kept the branded haptic taps on the splash.

## 4. Push notifications — made functional ✅
- New **server-side push sender** (`admin/lib/push.ts`) posting to Expo's Push API (sound + high priority); wired into **attendance & overtime approve/reject** so the employee gets a real phone push. Added the Android notification channel.
- Note: remote push only works on an **EAS build**, not Expo Go (SDK 53 limitation — that's the red banner, not a bug).

## 5. Payroll PDF export ✅
- Branded, server-generated **`.pdf`** (`@react-pdf/renderer`) with the BuildOne logo. Filter-driven scope: 1 entity/all users, 1 user/all entities, all users/1 entity, all users/all entities. Download buttons on the Payroll list, filtered bar, and run detail page.

## 6. Fixes ✅
- **Overtime filter** was showing stale results (client cached the list in state) — now syncs on prop change so employee/company/date filters work.
- Overtime decisions used a non-existent `reviewed_at` column → corrected to **`decided_at`** (approve/reject persists + shows the date).
- **Login success splash** was getting stuck — switched to a hard navigation (`window.location.assign('/')`) so the fresh session cookie is used.
- **Chart line made thinner** (mas manipis) on the dashboard time-tracking overview.
- Approve/Archive/action buttons given readable **white text** on dark/red; Entities/Projects **Archive → red**; action columns aligned.
- Web login background image + dark overlay + running-glow login card.

## 7. Demo data ✅
- SQL seed (`seed_demo_week.sql`) — a full **Mon–Sun week** of clock-ins/outs, timesheets, and overtime for **james, eunice, Joshua** (all under one entity, with pay rates) for a video walkthrough.

## 8. Admin account ✅
- Created the **admin@buildone.au** admin account (Dashboard → Add user + `make_admin_profile.sql`). `buildone@gmail.com` is an employee and is correctly rejected from the admin portal.

---

## Xero — scopes confirmed
The payroll export needs only: **`app.connections`, `payroll.employees`, `payroll.timesheets`, `payroll.settings`** (+ the automatic `openid profile email offline_access`). Extra scopes enabled on the Xero app are harmless. `offline_access` is the critical one (token auto-refresh).

---

## Outstanding / Next steps
1. **Rebuild the APK** to see all mobile changes (Timevera name/icon, both-logo splash, show-password, haptics, push channel):
   `cd mobile && eas build -p android --profile preview`  — installs as **Timevera**, a new app (bundle ID changed).
2. **Run the SQL** in Supabase if not yet: `seed_demo_week.sql`; confirm migrations `0010`–`0013` are applied.
3. **Log in with `admin@buildone.au`** on the admin site (not the employee account).
4. **Set employee pay rates** before pay runs; recreate a run after any rate/approval change (snapshot behaviour).
5. Verify the metallic **Timevera logo/text** reads clearly on the dark sidebar/login — if the grey text is too faint on dark, provide a light-text version.
6. Wire the actual **Xero "Export to Xero"** push (needs the real AU org's earnings-rate names + employee email matching).
