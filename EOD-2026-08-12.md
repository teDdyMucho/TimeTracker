# End of Day — 12 August 2026
**Project:** Timevera (BuildOne) Time Tracker — Admin Portal + Mobile App

---

## Summary
Confirmed **iOS v1.0.2 is approved and live** (all the new client features), traced why the Archive button "wasn't working" to a **stalled Netlify deploy** (code was correct all along), added an **"On leave" option to the Add Attendance form** (admin marks an employee on leave, auto-approved), and set up an **employee-invite email edge function** — after SMTP hit a dead end (MFA + app passwords disabled), we settled on Microsoft Graph, now waiting on a Global Admin (Robbie) to grant consent.

---

## 1. iOS v1.0.2 — APPROVED & LIVE ✅
- App Store Connect shows **1.0.2 (build 4) "Ready for Distribution"** — approved.
- The live iOS app now has: **Leave requests, Switch Project mid-workday, session-reset fix, 12h auto clock-out**, plus messaging. Users get it via update.
- Install link unchanged: `https://apps.apple.com/ph/app/timevera/id6787161301`.

## 2. Archive/Deactivate "not working" — root cause found ✅
- Client reported the Projects Archive button still not working on the live site.
- Verified: the **code fix is correct** (`'archived'` enum, pushed 7–8 Aug) and the DB update **succeeds** when run directly. The **Leave tab is missing on the live site** too — proof the new code never deployed.
- **Root cause: Netlify auto-deploy stalled** — the last "Published" deploy is old. Fix: Netlify → Deploys → **Trigger deploy → Clear cache and deploy site**. Once it redeploys, Archive/Deactivate + Leave tab + all admin fixes go live. (No code change needed.)

## 3. Admin — "On leave" on the Add Attendance form ✅
- New **"On leave (auto-approved)"** toggle in Add Attendance. When on, the form swaps to leave fields (type: Annual/Sick/Personal/Unpaid, from/to dates, reason) and creates an **auto-approved** `leave_requests` record for that employee — so the admin can quickly mark who's on leave today.
- Uses the existing leave table (no migration). Files: `attendance/actions.ts`, `attendance/attendance-form.tsx`.

## 4. Employee invite email (edge function) — built, pending consent ⏳
Goal: when an admin creates an employee, email them their login details from Outlook (admin@buildonedesignconstruction.com.au) via a Supabase Edge Function.
- Built `supabase/functions/send-employee-invite` and wired `createEmployeeAction` to call it (best-effort — a mail failure never blocks account creation). Guarded by `INVITE_SHARED_SECRET`.
- **SMTP path abandoned:** the sender mailbox has **MFA on** and the tenant has **App Passwords disabled** → basic SMTP auth can't work.
- **Switched to Microsoft Graph** (client-credentials / app-only — unaffected by MFA). Registered Azure app **"Timevera"** (Client ID `d42aa1fd-…`, Tenant `9b7d7fff-…`), added Graph **Application** permission `Mail.Send`.
- **Blocker:** "Grant admin consent" is greyed out — the current account isn't a Global Admin (Robbie & Steven are). Robbie must approve via the adminconsent URL (one click).
- Committed all of it so it's ready; not yet live.

---

## Outstanding / Next steps
1. **Netlify: trigger a fresh deploy** (Clear cache and deploy) so all the admin fixes/leave tab go live — this resolves the "Archive still broken" report.
2. **Email invite:** (a) Robbie opens the adminconsent URL and clicks Accept; (b) create the Azure **client secret**; (c) `supabase secrets set` the 5 MS_* / INVITE vars + `functions deploy send-employee-invite`; (d) set `INVITE_SHARED_SECRET` in Netlify env. Then test by creating an employee.
3. **Android APK:** finish/download the rebuilt APK → upload to Google Drive → update `ANDROID_APK_FILE_ID` in the admin install page.
4. Still on hold: **Xero push** (wait for client to grant Xero connection).

---

## Notes
- Adminconsent URL for Robbie: `https://login.microsoftonline.com/9b7d7fff-1313-4600-a29c-d61c42dfd22f/adminconsent?client_id=d42aa1fd-b6e0-4a1d-bd88-676b9a5a6e32`
- The old SMTP_* edge-function secrets set earlier are unused now — safe to leave or overwrite with the MS_* Graph secrets.
- Admin changes deploy on push, but ONLY once Netlify actually builds — check the Deploys tab after pushing.
